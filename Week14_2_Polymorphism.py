# In OOP, polymorphism means the same function name (but different signature)
# being used for different types

# In python built-in function: len()
# we can use the len() with OOP polymorphism

# string A
stringA = "Python object-oriented programming"
print(len(stringA))

print()

# list A
listA = [45, 7, 7899, 90]
print(len(listA))
